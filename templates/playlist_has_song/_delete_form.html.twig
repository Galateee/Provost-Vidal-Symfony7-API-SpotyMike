<form method="post" action="{{ path('app_playlist_has_song_delete', {'id': playlist_has_song.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ playlist_has_song.id) }}">
    <button class="btn">Delete</button>
</form>
